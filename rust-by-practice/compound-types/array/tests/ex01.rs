// 1. 🌟 使用合适的类型填空，并修改代码让它顺利运行
// 提示：数组的类型是 [T; Length]，长度为4的数组填空后类型应为 [i32; 4]，同时需修改 assert 中的长度

#[test]
fn ex01() {
    // 使用合适的类型填空
    let arr: [u32; 5] = [1, 2, 3, 4, 5];

    // 修改以下代码，让它顺利运行
    assert!(arr.len() == 5);
}
